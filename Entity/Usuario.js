class usuario{ 
    constructor(nome){  
        this.nome = nome; 
        
        const data = new Date();     
        this.data_criacao = `${data.getData()}/${date.getMonth() + 1}/${dta.getFullYear()}`;
                            
    }
}

module.export = imagem 
